<% if current_user %>
  <%= link_to t(".index"), root_path%>
  <%= link_to t(".categories"), categories_path if current_user.has_role? :admin%>
  <%= link_to t(".logout"), destroy_user_session_path, data:{
    turbo_method: :delete, turbo_confirm:t('common.confirm') } %>
<% else %>
  <%= link_to t(".login"), new_user_session_path %>
  <%= link_to t(".signup"), new_user_registration_path %>
<%end %>